{
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build:parcel": "rimraf dist && parcel build ./src/index.html --no-source-maps",
    "build:vuic": "vuicc ./dist ../ui.vuic",
    "build": "yarn build:parcel && yarn build:vuic"
  },
  "dependencies": {
    "@tweakpane/core": "1.0.3",
    "lodash.throttle": "4.1.1",
    "parcel-bundler": "1.12.5",
    "rimraf": "5.0.7",
    "tweakpane": "3.0.2"
  },
  "devDependencies": {
    "typescript": "5.7.2"
  }
}
